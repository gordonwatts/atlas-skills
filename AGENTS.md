# Agents file for the altas-skills repo

## Python

If you need python, then create a local virtual environment with:

`uv venv --python=<python-version>`

And then activate the environment that you've created (in `.venv`).

If the `.venv` is already present, just activate that. Install all python packages there if you need them.
